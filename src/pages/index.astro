---
import PageLayout from '../layouts/PageLayout.astro';
import ServiceCard from '../components/ServiceCard.astro';

const services = [
  {
    title: "IT Support & Monitoring",
    description: "Your servers, desktops, networks, and cloud systems monitored and maintained. When something breaks, we fix it. Before you even notice, ideally.",
    icon: "support"
  },
  {
    title: "Microsoft 365 Migration & Management",
    description: "Moving to M365? Already there but it's a mess? We handle SharePoint migrations, Exchange migrations, Teams setup, and ongoing administration.",
    icon: "microsoft"
  },
  {
    title: "VoIP & Teams Calling",
    description: "Replace your legacy phone system with Microsoft Teams calling. Proper integration with your M365 environment, none of the messy third-party VoIP setups that break every update.",
    icon: "voip"
  },
  {
    title: "Network Infrastructure",
    description: "UniFi installations, VLAN configuration, structured cabling, WiFi that actually works everywhere. We design networks for businesses that need reliable connectivity.",
    icon: "network"
  },
  {
    title: "Website Management & Hosting",
    description: "Keep your website online, email working, and SSL certificates current. We handle domains, hosting, and all the backend configuration that breaks when someone tries to \"just update one thing quickly\".",
    icon: "website"
  },
  {
    title: "Custom Software Development",
    description: "Sometimes off-the-shelf software doesn't fit. We build applications that solve your specific workflow problems, not generic ones.",
    icon: "code"
  },
  {
    title: "IT Procurement & Licensing",
    description: "Hardware, software, licensing. We source it, you see the actual cost breakdown. No hidden markups, no vendor kickbacks.",
    icon: "procurement"
  },
  {
    title: "IT Consulting & Project Work",
    description: "Office move? System upgrade? New clinic or branch opening? We handle the IT infrastructure side of your projects from planning to installation.",
    icon: "consulting"
  },
  {
    title: "Business Intelligence & Data Analytics",
    description: "Turn your data into decisions. We build dashboards, reports, and analytics solutions that show you what's actually happening in your business without the usual BI consultant waffle.",
    icon: "analytics"
  }
];

const whyItems = [
  {
    title: "No Hidden Costs",
    description: "You get itemised invoices that actually make sense. Every licence, every hour, every piece of hardware. If we're marking something up, you'll see it. Most IT companies don't operate this way because it kills their margin. We're fine with that."
  },
  {
    title: "12 Years in the Industry",
    description: "Not some fly-by-night operation. Our team brings over 12 years of real enterprise and SMB experience with Microsoft environments, cloud infrastructure, and business systems. We've seen what works and what doesn't."
  },
  {
    title: "We Build Custom Solutions When Needed",
    description: "If there's software that solves your problem, we'll recommend it. If there isn't, we'll build it. Simple as that. Not everything needs a custom application, but some things do."
  },
  {
    title: "We Work With Other Specialists",
    description: "If you need security systems, telecoms, or other IT-adjacent work, we partner with people who are actually good at those things instead of pretending we do everything. Better results for you."
  }
];

const aiOutcomes = [
  {
    title: "Stop doing manual data entry.",
    description: "Invoices, patient forms, supplier documents automatically read and entered into your systems.",
    icon: "document"
  },
  {
    title: "Answer routine questions instantly.",
    description: "Common enquiries handled automatically. Complex issues routed to the right person.",
    icon: "chat"
  },
  {
    title: "Get answers from your data without IT.",
    description: "Ask questions in plain English, get actual answers. No waiting for reports.",
    icon: "chart"
  },
  {
    title: "Works with what you already have.",
    description: "Connects to your existing software. No ripping and replacing systems.",
    icon: "connect"
  }
];
---

<PageLayout
  title="Natred | IT Support & Honest IT Services Johannesburg | Microsoft 365 Specialists"
  description="Natred provides honest IT support, Microsoft 365 services, network infrastructure, and custom development for businesses in Johannesburg and across South Africa. No hidden costs, transparent pricing."
>
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-bg-grid">
      <div class="snake snake-1"></div>
      <div class="snake snake-2"></div>
      <div class="snake snake-3"></div>
    </div>
    <div class="container">
      <div class="hero-content">
        <div class="hero-text">
          <h1 class="animate-on-scroll fade-up">IT Support That Doesn't<br>BS You</h1>
          <p class="subtitle animate-on-scroll fade-up">Johannesburg-based Honest IT Services</p>
          <p class="hero-description animate-on-scroll fade-up">
            Most IT companies overcomplicate things to justify their invoices. Natred keeps it simple.
            Straight answers, transparent pricing, and IT that actually works.
            Microsoft 365, network infrastructure, migrations, and custom development for businesses across South Africa.
          </p>
          <div class="cta-group animate-on-scroll fade-up">
            <button class="btn btn-primary btn-lg" data-open-contact-modal>Book Free Assessment</button>
            <a href="#services" class="btn btn-secondary btn-lg">Our Services</a>
          </div>
        </div>
        <div class="hero-image animate-on-scroll fade-left">
          <div class="hero-visual">
            <div class="trust-card">
              <div class="trust-header">
                <svg class="trust-shield" viewBox="0 0 40 40" fill="none">
                  <path d="M20 4L34 10V20C34 28 28 34 20 38C12 34 6 28 6 20V10L20 4Z" stroke="#f26722" stroke-width="2" fill="rgba(242,103,34,0.08)"/>
                  <path d="M14 20L18 24L26 16" stroke="#f26722" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
                <div class="trust-header-text">
                  <span class="trust-title">Your IT, sorted.</span>
                  <span class="trust-subtitle">So you can focus on your business</span>
                </div>
              </div>
              <div class="trust-stats">
                <div class="trust-stat">
                  <span class="trust-stat-number">50+</span>
                  <span class="trust-stat-label">SA businesses trust us</span>
                </div>
                <div class="trust-stat">
                  <span class="trust-stat-number">&lt;15min</span>
                  <span class="trust-stat-label">Average response time</span>
                </div>
                <div class="trust-stat">
                  <span class="trust-stat-number">24/7</span>
                  <span class="trust-stat-label">Systems monitored</span>
                </div>
              </div>
              <div class="trust-checklist">
                <div class="trust-check-item">
                  <svg viewBox="0 0 20 20" fill="none"><circle cx="10" cy="10" r="9" fill="rgba(76,175,80,0.15)" stroke="#4CAF50" stroke-width="1.5"/><path d="M6 10L9 13L14 7" stroke="#4CAF50" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                  <span>You'll always know what you're paying for</span>
                </div>
                <div class="trust-check-item">
                  <svg viewBox="0 0 20 20" fill="none"><circle cx="10" cy="10" r="9" fill="rgba(76,175,80,0.15)" stroke="#4CAF50" stroke-width="1.5"/><path d="M6 10L9 13L14 7" stroke="#4CAF50" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                  <span>Every request gets a real human response</span>
                </div>
                <div class="trust-check-item">
                  <svg viewBox="0 0 20 20" fill="none"><circle cx="10" cy="10" r="9" fill="rgba(76,175,80,0.15)" stroke="#4CAF50" stroke-width="1.5"/><path d="M6 10L9 13L14 7" stroke="#4CAF50" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                  <span>You don't need to understand IT. That's our job.</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section class="services section section-dark" id="services">
    <div class="container">
      <div class="services-header">
        <h2>What we do</h2>
        <p class="services-subtitle">IT infrastructure, cloud services, and custom development for businesses that want things done right.</p>
      </div>
      <div class="services-grid">
        <div class="grid-line grid-line-h1"></div>
        <div class="grid-line grid-line-h2"></div>
        <div class="grid-line grid-line-v1"></div>
        <div class="grid-line grid-line-v2"></div>
        {services.map((service) => (
          <button type="button" class="service-card">
            <div class="service-content">
              <div class="service-icon">
                {service.icon === 'support' && (
                  <svg viewBox="0 0 32 32" fill="none"><rect x="4" y="6" width="24" height="18" rx="2" stroke="currentColor" stroke-width="1.5"/><circle cx="16" cy="15" r="4" stroke="currentColor" stroke-width="1.5"/><circle cx="16" cy="15" r="1.5" fill="currentColor"/></svg>
                )}
                {service.icon === 'microsoft' && (
                  <svg viewBox="0 0 32 32" fill="none"><rect x="4" y="4" width="11" height="11" fill="currentColor"/><rect x="17" y="4" width="11" height="11" fill="currentColor" opacity="0.5"/><rect x="4" y="17" width="11" height="11" fill="currentColor" opacity="0.5"/><rect x="17" y="17" width="11" height="11" fill="currentColor"/></svg>
                )}
                {service.icon === 'voip' && (
                  <svg viewBox="0 0 32 32" fill="none"><path d="M9 5c-1.5 0-3 1.5-3 3v3c0 10 8 17 18 17h3c1.5 0 3-1.5 3-3v-3c0-.5-.5-1.5-1.5-1.5h-4c-.5 0-1.5.5-1.5 1.5v1.5c-6-1.5-10-6-11-11H13.5c1 0 1.5-1 1.5-1.5v-4c0-1-.5-1.5-1.5-1.5H9z" stroke="currentColor" stroke-width="1.5" fill="none"/></svg>
                )}
                {service.icon === 'network' && (
                  <svg viewBox="0 0 32 32" fill="none"><circle cx="16" cy="6" r="4" stroke="currentColor" stroke-width="1.5"/><circle cx="6" cy="26" r="4" stroke="currentColor" stroke-width="1.5"/><circle cx="26" cy="26" r="4" stroke="currentColor" stroke-width="1.5"/><path d="M16 10v6M12 20l-4 4M20 20l4 4" stroke="currentColor" stroke-width="1.5"/><circle cx="16" cy="18" r="2" fill="currentColor"/></svg>
                )}
                {service.icon === 'website' && (
                  <svg viewBox="0 0 32 32" fill="none"><rect x="4" y="6" width="24" height="18" rx="2" stroke="currentColor" stroke-width="1.5"/><path d="M4 11h24" stroke="currentColor" stroke-width="1.5"/><circle cx="7" cy="8.5" r="1" fill="currentColor"/><circle cx="10" cy="8.5" r="1" fill="currentColor"/></svg>
                )}
                {service.icon === 'code' && (
                  <svg viewBox="0 0 32 32" fill="none"><path d="M11 10L5 16l6 6M21 10l6 6-6 6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M18 6l-4 20" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" opacity="0.5"/></svg>
                )}
                {service.icon === 'procurement' && (
                  <svg viewBox="0 0 32 32" fill="none"><rect x="6" y="8" width="20" height="18" rx="2" stroke="currentColor" stroke-width="1.5"/><path d="M11 8V5h10v3" stroke="currentColor" stroke-width="1.5"/><path d="M11 16h10M11 21h6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" opacity="0.5"/></svg>
                )}
                {service.icon === 'consulting' && (
                  <svg viewBox="0 0 32 32" fill="none"><circle cx="16" cy="10" r="5" stroke="currentColor" stroke-width="1.5"/><path d="M8 26c0-5 3-8 8-8s8 3 8 8" stroke="currentColor" stroke-width="1.5"/><path d="M22 8l4-2M22 12l4 1" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" opacity="0.5"/></svg>
                )}
                {service.icon === 'analytics' && (
                  <svg viewBox="0 0 32 32" fill="none"><rect x="4" y="4" width="24" height="24" rx="2" stroke="currentColor" stroke-width="1.5"/><path d="M9 22V14M15 22V10M21 22V16" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
                )}
              </div>
              <h3>{service.title}</h3>
              <p>{service.description}</p>
            </div>
            <span class="service-arrow">Learn more <span class="arrow-icon">&rarr;</span></span>
          </button>
        ))}
      </div>
      <!-- Floating hint that appears once -->
      <div class="services-hint" id="services-hint">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 15l-2 5L9 9l11 4-5 2z"/><path d="M21 3l-6.5 6.5"/></svg>
        Click any service for details
      </div>
    </div>
  </section>

  <!-- AI Section -->
  <section class="ai-section" id="ai">
    <div class="container">
      <div class="ai-content">
        <h2 class="animate-on-scroll fade-up">AI That Actually Helps Your Business</h2>
        <p class="ai-subtitle animate-on-scroll fade-up">Not hype. Practical automation that saves time.</p>
        <p class="ai-description animate-on-scroll fade-up">Your team spends hours on tasks that shouldn't need a person. Sorting through invoices, answering the same customer questions, pulling reports from five different systems, checking forms for errors.</p>
        <p class="ai-description animate-on-scroll fade-up">We set up AI to handle this repetitive work so your people can focus on what actually matters.</p>

        <div class="ai-outcomes stagger-children">
          {aiOutcomes.map(outcome => (
            <div class="ai-outcome">
              <div class="ai-outcome-icon">
                {outcome.icon === 'document' && (
                  <svg viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <rect x="12" y="8" width="40" height="48" rx="2" stroke="#f26722" stroke-width="3" fill="none"/>
                    <line x1="20" y1="18" x2="44" y2="18" stroke="#888" stroke-width="2"/>
                    <line x1="20" y1="26" x2="44" y2="26" stroke="#888" stroke-width="2"/>
                    <line x1="20" y1="34" x2="38" y2="34" stroke="#888" stroke-width="2"/>
                    <circle cx="48" cy="48" r="12" fill="#f26722"/>
                    <path d="M 43 48 L 46 51 L 53 44" stroke="#fff" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
                  </svg>
                )}
                {outcome.icon === 'chat' && (
                  <svg viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M 16 32 Q 16 16 32 16 Q 48 16 48 32 Q 48 40 42 44 L 42 52 L 38 52 L 38 48 L 26 48 L 26 52 L 22 52 L 22 44 Q 16 40 16 32 Z" fill="#f26722"/>
                    <circle cx="26" cy="28" r="2" fill="#fff"/>
                    <circle cx="38" cy="28" r="2" fill="#fff"/>
                    <path d="M 28 36 Q 32 38 36 36" stroke="#fff" stroke-width="2" stroke-linecap="round" fill="none"/>
                  </svg>
                )}
                {outcome.icon === 'chart' && (
                  <svg viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <rect x="8" y="12" width="48" height="40" rx="2" stroke="#f26722" stroke-width="3" fill="none"/>
                    <line x1="16" y1="44" x2="16" y2="24" stroke="#888" stroke-width="2"/>
                    <line x1="14" y1="44" x2="54" y2="44" stroke="#888" stroke-width="2"/>
                    <polyline points="20,38 26,32 32,36 38,28 44,32 48,26" stroke="#f26722" stroke-width="3" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                )}
                {outcome.icon === 'connect' && (
                  <svg viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <circle cx="20" cy="20" r="12" stroke="#f26722" stroke-width="3" fill="none"/>
                    <circle cx="44" cy="20" r="12" stroke="#f26722" stroke-width="3" fill="none"/>
                    <circle cx="20" cy="44" r="12" stroke="#f26722" stroke-width="3" fill="none"/>
                    <circle cx="44" cy="44" r="12" stroke="#f26722" stroke-width="3" fill="none"/>
                    <line x1="28" y1="24" x2="36" y2="28" stroke="#888" stroke-width="2.5"/>
                    <line x1="28" y1="40" x2="36" y2="36" stroke="#888" stroke-width="2.5"/>
                    <line x1="24" y1="28" x2="24" y2="36" stroke="#888" stroke-width="2.5"/>
                    <line x1="40" y1="28" x2="40" y2="36" stroke="#888" stroke-width="2.5"/>
                  </svg>
                )}
              </div>
              <div class="ai-outcome-content">
                <h3>{outcome.title}</h3>
                <p>{outcome.description}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Why Us Section -->
  <section class="why-us section section-dark" id="why">
    <div class="container">
      <h2 class="section-title animate-on-scroll fade-up">Why We're Different</h2>
      <div class="why-us-content stagger-children">
        {whyItems.map(item => (
          <div class="why-item">
            <h3>{item.title}</h3>
            <p>{item.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Testimonial Section -->
  <section class="testimonial-section section-charcoal">
    <div class="container">
      <div class="testimonial-content animate-on-scroll scale">
        <p class="testimonial-quote">Natred's services were competitively priced, and throughout the process we felt confident that our IT infrastructure was in knowledgeable, capable hands. As a medical facility with complex billing, compliance, and record-keeping requirements, reliable technology is not optional...</p>
        <button class="read-more-btn" data-open-testimonial>Read full testimonial</button>
        <div class="testimonial-attribution">
          <span class="testimonial-name">Jude Polack</span> — <span class="testimonial-title">Director, Genesis Clinic</span>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section section section-dark" id="contact">
    <div class="container">
      <div class="cta-content animate-on-scroll fade-up">
        <h2>Need IT Support?</h2>
        <p>If you're tired of IT companies that overcomplicate things or hide costs, let's talk. Whether it's a migration project, ongoing support, or a one-off infrastructure job.</p>
        <button class="btn btn-primary btn-lg" data-open-contact-modal>Get In Touch</button>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  /* Hero Section */
  .hero {
    padding: 100px 0 80px;
    background: var(--charcoal);
    position: relative;
    overflow: hidden;
  }

  .hero-bg-grid {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    overflow: hidden;
    pointer-events: none;
  }

  /* Static grid background */
  .hero-bg-grid::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image:
      linear-gradient(rgba(242, 103, 34, 0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(242, 103, 34, 0.05) 1px, transparent 1px);
    background-size: 60px 60px;
  }

  /* Snake lights - glowing dots with trails */
  .snake {
    position: absolute;
    width: 6px;
    height: 6px;
    background: #f26722;
    border-radius: 50%;
    box-shadow:
      0 0 6px 2px #f26722,
      0 0 12px 4px rgba(242, 103, 34, 0.6),
      0 0 20px 6px rgba(242, 103, 34, 0.3);
    opacity: 0;
  }

  /* Snake 1: enters left, wanders right with turns */
  .snake-1 {
    animation: snake1 14s ease-in-out infinite;
  }

  @keyframes snake1 {
    0% { left: -10px; top: 120px; opacity: 0; }
    2% { left: 0px; top: 120px; opacity: 1; }

    15% { left: 240px; top: 120px; }
    20% { left: 300px; top: 120px; }
    25% { left: 300px; top: 180px; }
    30% { left: 300px; top: 240px; }

    40% { left: 420px; top: 240px; }
    45% { left: 480px; top: 240px; }
    50% { left: 480px; top: 300px; }

    60% { left: 600px; top: 300px; }
    65% { left: 660px; top: 300px; }
    70% { left: 660px; top: 240px; }
    75% { left: 660px; top: 180px; }

    85% { left: 780px; top: 180px; }
    95% { left: 900px; top: 180px; opacity: 1; }
    100% { left: 960px; top: 180px; opacity: 0; }
  }

  /* Snake 2: enters top, moves down and across */
  .snake-2 {
    animation: snake2 16s ease-in-out infinite;
    animation-delay: 5s;
  }

  @keyframes snake2 {
    0% { left: 540px; top: -10px; opacity: 0; }
    2% { left: 540px; top: 0px; opacity: 1; }

    12% { left: 540px; top: 120px; }
    18% { left: 540px; top: 180px; }

    28% { left: 420px; top: 180px; }
    35% { left: 360px; top: 180px; }
    42% { left: 360px; top: 300px; }

    52% { left: 240px; top: 300px; }
    60% { left: 180px; top: 300px; }
    68% { left: 180px; top: 360px; }
    75% { left: 180px; top: 420px; }

    85% { left: 60px; top: 420px; }
    95% { left: 0px; top: 420px; opacity: 1; }
    100% { left: -10px; top: 420px; opacity: 0; }
  }

  /* Snake 3: enters right, wanders left */
  .snake-3 {
    animation: snake3 12s ease-in-out infinite;
    animation-delay: 9s;
  }

  @keyframes snake3 {
    0% { left: 1260px; top: 240px; opacity: 0; }
    3% { left: 1200px; top: 240px; opacity: 1; }

    18% { left: 1020px; top: 240px; }
    25% { left: 960px; top: 240px; }
    32% { left: 960px; top: 180px; }
    40% { left: 960px; top: 120px; }

    52% { left: 840px; top: 120px; }
    62% { left: 780px; top: 120px; }
    72% { left: 780px; top: 180px; }

    85% { left: 660px; top: 180px; }
    95% { left: 540px; top: 180px; opacity: 1; }
    100% { left: 480px; top: 180px; opacity: 0; }
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .hero-text {
    max-width: 600px;
  }

  .hero h1 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 700;
    margin-bottom: 20px;
    line-height: 1.1;
    letter-spacing: -0.02em;
  }

  .subtitle {
    font-size: 20px;
    color: var(--light-gray);
    margin-bottom: 30px;
    font-weight: 300;
  }

  .hero-description {
    font-size: 18px;
    max-width: 800px;
    line-height: 1.8;
    margin-bottom: 30px;
    color: var(--light-gray);
    font-weight: 300;
  }

  .cta-group {
    display: flex;
    gap: 15px;
    flex-wrap: wrap;
    align-items: center;
  }

  .hero-image {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-visual {
    width: 100%;
    max-width: 420px;
  }

  /* Trust card */
  .trust-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--dark-gray);
    border-radius: 0;
    padding: 40px;
    display: flex;
    flex-direction: column;
    gap: 28px;
  }

  .trust-header {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .trust-shield {
    width: 44px;
    height: 44px;
    flex-shrink: 0;
  }

  .trust-header-text {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .trust-title {
    font-size: 20px;
    font-weight: 600;
    color: var(--white);
  }

  .trust-subtitle {
    font-size: 14px;
    color: var(--light-gray);
    font-weight: 300;
  }

  .trust-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }

  .trust-stat {
    text-align: center;
    padding: 16px 8px;
    background: rgba(242, 103, 34, 0.04);
    border: 1px solid rgba(242, 103, 34, 0.12);
    border-radius: 0;
  }

  .trust-stat-number {
    display: block;
    font-size: 24px;
    font-weight: 700;
    color: var(--natred-orange);
    line-height: 1.2;
  }

  .trust-stat-label {
    display: block;
    font-size: 11px;
    color: var(--light-gray);
    margin-top: 4px;
    line-height: 1.3;
  }

  .trust-checklist {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .trust-check-item {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 15px;
    color: var(--light-gray);
    font-weight: 300;
  }

  .trust-check-item svg {
    width: 22px;
    height: 22px;
    flex-shrink: 0;
  }


  /* Services - Clean Modern Style */
  .services-header {
    margin-bottom: 60px;
  }

  .services-header h2 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 600;
    margin-bottom: 16px;
    background: linear-gradient(to right, var(--white), var(--light-gray));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .services-subtitle {
    font-size: 18px;
    color: var(--light-gray);
    max-width: 500px;
    line-height: 1.6;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-auto-rows: 1fr;
    gap: 1px;
    background: var(--dark-gray);
    position: relative;
  }

  /* Snake lights along grid lines */
  .grid-line {
    position: absolute;
    z-index: 5;
    pointer-events: none;
    overflow: hidden;
    background: var(--dark-gray);
  }

  .grid-line::after {
    content: '';
    position: absolute;
    background: linear-gradient(90deg, transparent, var(--natred-orange), transparent);
    border-radius: 2px;
  }

  .grid-line-h1, .grid-line-h2 {
    left: 0;
    right: 0;
    height: 1px;
  }

  .grid-line-h1 { top: calc(100% / 3); }
  .grid-line-h2 { top: calc(100% / 3 * 2); }

  .grid-line-v1, .grid-line-v2 {
    top: 0;
    bottom: 0;
    width: 1px;
  }

  .grid-line-v1 { left: calc(100% / 3); }
  .grid-line-v2 { left: calc(100% / 3 * 2); }

  .grid-line-h1::after, .grid-line-h2::after {
    width: 60px;
    height: 3px;
    top: -1px;
    animation: snake-h 6s linear infinite;
  }

  .grid-line-h2::after {
    animation-delay: -3s;
  }

  .grid-line-v1::after, .grid-line-v2::after {
    width: 3px;
    height: 60px;
    left: -1px;
    background: linear-gradient(180deg, transparent, var(--natred-orange), transparent);
    animation: snake-v 6s linear infinite;
  }

  .grid-line-v2::after {
    animation-delay: -3s;
  }

  @keyframes snake-h {
    0% { left: -60px; opacity: 0; }
    5% { opacity: 1; }
    95% { opacity: 1; }
    100% { left: 100%; opacity: 0; }
  }

  @keyframes snake-v {
    0% { top: -60px; opacity: 0; }
    5% { opacity: 1; }
    95% { opacity: 1; }
    100% { top: 100%; opacity: 0; }
  }

  .service-card {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: stretch;
    text-align: left;
    background: var(--black);
    padding: 40px;
    min-height: 280px;
    text-decoration: none;
    position: relative;
    overflow: hidden;
    transition: background 0.3s ease;
    border: none;
    cursor: pointer;
    font-family: inherit;
    color: inherit;
  }

  .service-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(242, 103, 34, 0.08) 0%, transparent 50%);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .service-card:hover {
    background: var(--charcoal);
  }

  .service-card:hover::before {
    opacity: 1;
  }

  .service-content {
    position: relative;
    z-index: 1;
  }

  .service-icon {
    width: 32px;
    height: 32px;
    color: var(--natred-orange);
    margin-bottom: 20px;
    transition: transform 0.3s ease;
  }

  .service-card:hover .service-icon {
    transform: scale(1.1);
  }

  .service-card h3 {
    font-size: 20px;
    font-weight: 600;
    color: var(--white);
    margin-bottom: 16px;
    transition: color 0.2s ease;
  }

  .service-card:hover h3 {
    color: var(--natred-orange);
  }

  .service-card p {
    font-size: 15px;
    line-height: 1.7;
    color: var(--light-gray);
    font-weight: 300;
  }

  .service-arrow {
    font-size: 13px;
    font-weight: 500;
    color: var(--dark-gray);
    transition: all 0.3s ease;
    align-self: flex-end;
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .arrow-icon {
    font-size: 18px;
    transition: transform 0.3s ease;
  }

  .service-card:hover .service-arrow {
    color: var(--natred-orange);
  }

  .service-card:hover .arrow-icon {
    transform: translateX(4px);
  }

  /* Floating hint chip */
  .services-hint {
    display: flex;
    align-items: center;
    gap: 8px;
    justify-content: center;
    margin-top: 30px;
    padding: 10px 20px;
    background: var(--charcoal);
    border: 1px solid var(--dark-gray);
    color: var(--light-gray);
    font-size: 14px;
    font-weight: 400;
    opacity: 0;
    transform: translateY(10px);
    transition: opacity 0.5s ease, transform 0.5s ease;
    width: fit-content;
    margin-left: auto;
    margin-right: auto;
  }

  .services-hint.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .services-hint.fade-out {
    opacity: 0;
    transform: translateY(-5px);
  }

  .services-hint svg {
    color: var(--natred-orange);
    flex-shrink: 0;
  }


  /* AI Section */
  .ai-section {
    padding: 100px 0;
    background: var(--charcoal);
    border-top: 2px solid var(--natred-orange);
    border-bottom: 2px solid var(--natred-orange);
  }

  .ai-content {
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
  }

  .ai-section h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    margin-bottom: 25px;
  }

  .ai-subtitle {
    font-size: 22px;
    color: var(--natred-orange);
    margin-bottom: 30px;
    font-weight: 500;
  }

  .ai-description {
    font-size: 18px;
    line-height: 1.8;
    color: var(--light-gray);
    margin-bottom: 20px;
  }

  .ai-outcomes {
    margin-top: 60px;
    text-align: center;
  }

  .ai-outcome {
    margin-bottom: 45px;
    display: flex;
    align-items: flex-start;
    gap: 25px;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .ai-outcome-icon {
    flex-shrink: 0;
    width: 60px;
    height: 60px;
  }

  .ai-outcome-content {
    flex: 1;
    text-align: left;
  }

  .ai-outcome h3 {
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 700;
    margin-bottom: 10px;
    color: var(--white);
  }

  .ai-outcome p {
    font-size: 17px;
    margin: 0;
    color: var(--light-gray);
  }

  /* Why Us Section */
  .why-us-content {
    max-width: 900px;
    margin: 0 auto;
  }

  .why-item {
    margin-bottom: 40px;
    padding-bottom: 40px;
    border-bottom: 1px solid var(--dark-gray);
  }

  .why-item:last-child {
    border-bottom: none;
    margin-bottom: 0;
    padding-bottom: 0;
  }

  .why-item h3 {
    font-size: 24px;
    margin-bottom: 12px;
    color: var(--white);
    font-weight: 600;
  }

  .why-item h3::before {
    content: "\2192";
    color: var(--natred-orange);
    margin-right: 12px;
    font-weight: 700;
  }

  .why-item p {
    color: var(--light-gray);
    line-height: 1.8;
    font-weight: 300;
    font-size: 17px;
  }

  /* Testimonial Section */
  .testimonial-section {
    padding: 100px 0;
  }

  .testimonial-content {
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
  }

  .testimonial-quote {
    font-size: clamp(1.25rem, 2.5vw, 1.75rem);
    line-height: 1.6;
    font-weight: 400;
    color: var(--white);
    margin-bottom: 35px;
    font-style: italic;
  }

  .testimonial-quote::before {
    content: '\201C';
    color: var(--natred-orange);
  }

  .testimonial-quote::after {
    content: '\201D';
    color: var(--natred-orange);
  }

  .testimonial-attribution {
    font-size: 16px;
    color: var(--light-gray);
  }

  .testimonial-name {
    color: var(--white);
    font-weight: 600;
  }

  .testimonial-title {
    color: var(--natred-orange);
  }

  .read-more-btn {
    background: transparent;
    border: none;
    color: var(--natred-orange);
    font-size: 15px;
    font-weight: 500;
    cursor: pointer;
    padding: 0;
    margin-bottom: 25px;
    transition: color 0.1s ease-out;
  }

  .read-more-btn:hover {
    color: var(--white);
  }

  .read-more-btn::after {
    content: ' \2192';
  }

  /* CTA Section */
  .cta-section {
    text-align: center;
    border-top: 2px solid var(--dark-gray);
  }

  .cta-content h2 {
    font-size: clamp(2rem, 4vw, 2.625rem);
    margin-bottom: 20px;
  }

  .cta-content p {
    font-size: 18px;
    color: var(--light-gray);
    margin-bottom: 40px;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.7;
  }

  /* Mobile Styles */
  @media (max-width: 768px) {
    .hero {
      padding: 40px 0 60px;
    }

    .hero-content {
      grid-template-columns: 1fr;
      gap: 30px;
    }

    .hero h1 {
      font-size: 2.2rem;
    }

    .hero-description {
      font-size: 15px;
      margin-bottom: 20px;
    }

    .trust-card {
      padding: 32px 24px;
      gap: 20px;
    }

    .trust-stats {
      gap: 10px;
    }

    .trust-stat-number {
      font-size: 20px;
    }

    .trust-stat-label {
      font-size: 10px;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .grid-line {
      display: none;
    }

    .service-card {
      padding: 32px 24px;
      min-height: auto;
    }


    .ai-section {
      padding: 70px 0;
    }

    .ai-outcome {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .ai-outcome-content {
      text-align: center;
    }

    .testimonial-section {
      padding: 70px 0;
    }

    .why-item {
      margin-bottom: 32px;
      padding-bottom: 32px;
    }

    .cta-group {
      flex-direction: column;
      width: 100%;
    }

    .cta-group .btn {
      width: 100%;
    }

    .snake {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .snake {
      display: none;
    }

    .scroll-hint {
      animation: none;
    }

    .services-header {
      margin-bottom: 40px;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .grid-line {
      display: none;
    }

    .service-card {
      padding: 32px 24px;
      min-height: auto;
    }
  }

  /* ===== Light Mode Overrides ===== */

  /* Trust card background */
  :global([data-theme="light"]) .trust-card {
    background: rgba(0, 0, 0, 0.02);
  }

  /* Trust stat backgrounds */
  :global([data-theme="light"]) .trust-stat {
    background: rgba(242, 103, 34, 0.06);
    border-color: rgba(242, 103, 34, 0.15);
  }

  /* Snake glow — less intense on light bg */
  :global([data-theme="light"]) .snake {
    box-shadow:
      0 0 6px 2px #f26722,
      0 0 12px 4px rgba(242, 103, 34, 0.4),
      0 0 20px 6px rgba(242, 103, 34, 0.15);
  }

  /* Hero grid lines lighter */
  :global([data-theme="light"]) .hero-bg-grid::before {
    background-image:
      linear-gradient(rgba(242, 103, 34, 0.08) 1px, transparent 1px),
      linear-gradient(90deg, rgba(242, 103, 34, 0.08) 1px, transparent 1px);
  }

  /* Service card gradient overlay — less dramatic on light bg */
  :global([data-theme="light"]) .service-card::before {
    background: linear-gradient(135deg, rgba(242, 103, 34, 0.05) 0%, transparent 50%);
  }

  /* Services grid separator color */
  :global([data-theme="light"]) .services-grid {
    background: var(--dark-gray);
  }

  /* Mobile menu shadow lighter */
  :global([data-theme="light"]) .nav-menu {
    box-shadow: -5px 0 20px rgba(0, 0, 0, 0.1);
  }
</style>

<script>
  // Show hint when services section enters viewport, then fade out
  const hint = document.getElementById('services-hint');
  const servicesSection = document.getElementById('services');

  if (hint && servicesSection) {
    const hintObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          hint.classList.add('visible');
          // Fade out after 4 seconds
          setTimeout(() => {
            hint.classList.add('fade-out');
          }, 4000);
          hintObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.3 });

    hintObserver.observe(servicesSection);

    // Also hide hint immediately if a service card is clicked
    document.querySelectorAll('.service-card').forEach(card => {
      card.addEventListener('click', () => {
        hint.classList.add('fade-out');
      });
    });
  }
</script>
